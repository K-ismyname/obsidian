# 1. Plan 
```
@docs/spec.md 와 @CLAUDE.md 를 먼저 읽어라.

목표: 이 서비스의 데이터 모델과 프로젝트 골격을 설계한다.
- Supabase(Postgres) 테이블: users(닉네임 가입), timelines(5단계 + 사전준비
  단계별 날짜, 공유 여부), posts(부서별 게시판)
- FastAPI 골격: app/main.py, app/clients/(외부 API 래퍼), app/parsers/
- LangGraph 골격: [단계 판정] → [조건 분기] → [알림 / 비자불레틴 체크] 노드

제약:
- CLAUDE.md의 Rules를 모두 지켜라(특히 외부 API는 app/clients 래퍼 경유, TDD).
- 5단계 정의는 spec.md를 정본으로 삼아라.

지금은 코드를 작성하지 마라. 다음을 계획으로 먼저 제시해라:
1. 테이블 스키마 (컬럼/타입/관계) 초안
2. 디렉토리·파일 구조
3. 어떤 순서로 만들지 단계별 작업 목록
4. 설계상 고민되는 지점이나 대안이 있으면 2~3개 옵션으로 비교
```